services:
  erp:
    build: .
    container_name: erp-demo
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:5051:5000"
    volumes:
      - ./ca.pem:/app/ca.pem:ro
